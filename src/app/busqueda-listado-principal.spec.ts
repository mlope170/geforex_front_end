import { BusquedaListadoPrincipal } from './busqueda-listado-principal';

describe('BusquedaListadoPrincipal', () => {
  it('should create an instance', () => {
    expect(new BusquedaListadoPrincipal()).toBeTruthy();
  });
});
